%% Input set id.
-define(SET_INPUT, {<<"set_input">>, ps_aworset}).

%% Result group_by set id.
-define(GROUP_BY_SET_INPUT, {<<"group_by_set_input">>, ps_group_by_orset}).

%% A counter for completed clients.
-define(
    COUNTER_COMPLETED_CLIENTS, {<<"counter_completed_clients">>, ps_gcounter}).
